# crawler/.env — 이 파일을 crawler/.env 로 복사한 뒤 값을 채우세요. (.env 는 git에 올리지 마세요)

# 오늘의딜 ingest 엔드포인트 (운영). 로컬 테스트 시 http://localhost:3000/api/deals/ingest
INGEST_URL=https://todaydeals.co.kr/api/deals/ingest

# 서버에 설정한 CRON_SECRET 과 동일한 값 (ingest 인증키)
INGEST_SECRET=
